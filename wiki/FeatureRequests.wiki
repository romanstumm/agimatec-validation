#summary TO DOs and missing features

  * !GroupSequence, Groups are most likely not handled correctly
  * Improve interaction between annotation based validation (jsr 303) and non-annotation based validations (agimatec)
  * More tests (ongoing)
  * Tutorial / real-world example
  * Clarify access to overwritten getter-methods (private/protected/public), value-access for annotated setter-methods (are annotated setters supported?), field-access for private fields that are overloaded in subclasses